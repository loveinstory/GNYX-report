P08 样例 PDF 暂未放入仓库。

当前版本已基于 `hebing22757.pdf` 的完整 OCR JSON 形态完成 v0.2 策略校准：

- 支持 `report_info + pages[].test_items` 专业结构化输入。
- 支持 9 个源检验项目：NT-proBNP、血管紧张素I 37℃、血管紧张素I 4℃、血管紧张素II、血浆肾素活性、血醛固酮/血浆肾素活性、血醛固酮、D-二聚体、游离脂肪酸。
- 自动按单位换算生成 Ang II/Ang I 比值，作为当前模板的 RAAS 比值展示位。

为避免真实检测数据进入仓库，暂不保存完整 OCR JSON 样例文件；后续如需回归测试，可使用脱敏后的 fixture。
